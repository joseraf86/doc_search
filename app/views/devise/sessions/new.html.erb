<div style="margin-left:-70px">

<%= titulo 'Inicio de Sesión' %>
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => { :class => "form-horizontal", :style => ""  } ) do |f| %>
  <div class="control-group">
    <%= f.label :nombre, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :nombre, :placeholder => "Nombre", :autofocus => true, :autocomplete => 'off' %>
    </div>
  </div>

  <div class="control-group">
    <%= f.label :contraseña, :class => "control-label", :for => "inputPassword" %>
    <div class="controls">
      <%= f.password_field :password, :placeholder => "Contraseña" %>
    </div>
  </div>

  <div class="control-group">
    <div class="controls">
      <% if devise_mapping.rememberable? -%>
        <%= f.check_box :remember_me %> <%= f.label :remember_me %>
      <% end -%>
      <%= f.submit "Iniciar sesión", :class => 'btn', :style => '' %><br/>
      <%= render "devise/shared/links" %>
    </div>
  </div>

<% end %>

</div>


